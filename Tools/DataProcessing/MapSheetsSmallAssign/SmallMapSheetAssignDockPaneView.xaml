<UserControl x:Class="XIAOFUTools.Tools.MapSheetsSmallAssign.SmallMapSheetAssignDockPaneView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/XIAOFUTools;component/Styles/ControlStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="70" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="面图层:" VerticalAlignment="Center" Style="{StaticResource FormLabelStyle}" />
            <ComboBox Grid.Column="1"
                      ItemsSource="{Binding PolygonLayers}"
                      SelectedItem="{Binding SelectedPolygonLayer}"
                      DisplayMemberPath="Name"
                      Style="{StaticResource ComboBoxStyle}" />
            <Button Grid.Column="2"
                    Content="刷新"
                    Margin="10,0,0,0"
                    MinWidth="60"
                    Command="{Binding RefreshLayersCommand}"
                    Style="{StaticResource DefaultButtonStyle}" />
        </Grid>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="70" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="目标字段:" VerticalAlignment="Center" Style="{StaticResource FormLabelStyle}" />
            <ComboBox Grid.Column="1"
                      ItemsSource="{Binding TargetFields}"
                      SelectedItem="{Binding SelectedTargetField}"
                      DisplayMemberPath="DisplayName"
                      Style="{StaticResource ComboBoxStyle}" />
        </Grid>

        <Grid Grid.Row="2" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="70" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="比例尺:" VerticalAlignment="Center" Style="{StaticResource FormLabelStyle}" />
            <ComboBox Grid.Column="1"
                      ItemsSource="{Binding ScaleNames}"
                      SelectedItem="{Binding SelectedScaleName}"
                      Style="{StaticResource ComboBoxStyle}" />
        </Grid>

        <Grid Grid.Row="3" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="70" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="地图查询:" VerticalAlignment="Center" Style="{StaticResource FormLabelStyle}" />
            <TextBlock Grid.Column="1"
                       Text="{Binding IdentifyStatusText}"
                       VerticalAlignment="Center"
                       TextWrapping="Wrap" />
            <Button Grid.Column="2"
                    Content="开始查询"
                    Margin="10,0,0,0"
                    MinWidth="80"
                    Command="{Binding ActivateIdentifyCommand}"
                    Style="{StaticResource DefaultButtonStyle}" />
            <Button Grid.Column="3"
                    Content="退出查询"
                    Margin="10,0,0,0"
                    MinWidth="80"
                    Command="{Binding StopIdentifyCommand}"
                    Style="{StaticResource CancelButtonStyle}"
                    IsEnabled="{Binding IsIdentifyModeActive}" />
        </Grid>

        <Grid Grid.Row="4" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="70" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="查询结果:" VerticalAlignment="Center" Style="{StaticResource FormLabelStyle}" />
            <TextBlock Grid.Column="1"
                       Text="{Binding IdentifyResultText}"
                       VerticalAlignment="Center"
                       TextWrapping="Wrap" />
        </Grid>

        <ProgressBar Grid.Row="5"
                     Height="6"
                     Margin="0,0,0,10"
                     IsIndeterminate="{Binding IsProcessing}"
                     Visibility="{Binding IsProcessing, Converter={StaticResource BooleanToVisibilityConverter}}"
                     Style="{StaticResource ProgressBarStyle}" />

        <Border Grid.Row="6" BorderBrush="#CDCDCD" BorderThickness="1" Margin="0,0,0,10">
            <TextBox Text="{Binding LogContent}"
                     BorderThickness="0"
                     IsReadOnly="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     Style="{StaticResource LogTextBoxStyle}" />
        </Border>

        <Border Grid.Row="7"
                BorderBrush="{StaticResource DividerBrush}"
                BorderThickness="0,1,0,0"
                Padding="0,10,0,0">
            <Grid>
                <Button Content="?"
                        Width="22"
                        Height="22"
                        Style="{StaticResource HelpButtonStyle}"
                        Command="{Binding ShowHelpCommand}"
                        HorizontalAlignment="Left" />

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="停止"
                            Width="80"
                            Margin="0,0,10,0"
                            Command="{Binding CancelCommand}"
                            Style="{StaticResource CancelButtonStyle}"
                            Visibility="{Binding IsProcessing, Converter={StaticResource BooleanToVisibilityConverter}}" />
                    <Button Content="运行"
                            Width="80"
                            Command="{Binding RunCommand}"
                            Style="{StaticResource ExecuteButtonStyle}"
                            IsEnabled="{Binding CanRun}" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
