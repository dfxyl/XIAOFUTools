<UserControl x:Class="XIAOFUTools.Tools.BoundaryPointGenerator.BoundaryPointGeneratorDockPaneView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:extensions="clr-namespace:ArcGIS.Desktop.Extensions;assembly=ArcGIS.Desktop.Extensions"
             xmlns:local="clr-namespace:XIAOFUTools.Tools.BoundaryPointGenerator"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="400"
             d:DataContext="{Binding Path=BoundaryPointGeneratorDockPaneViewModel}"
             Loaded="UserControl_Loaded">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/XIAOFUTools;component/Styles/ControlStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- 布尔值转文本转换器 -->
            <local:BooleanToTextConverter x:Key="BooleanToTextConverter"/>

            <!-- 布尔值转可见性转换器 -->
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

            <!-- 紧凑型下拉框样式（本地） -->
            <Style x:Key="CompactComboBoxStyle" TargetType="ComboBox" BasedOn="{StaticResource ComboBoxStyle}">
                <Setter Property="Margin" Value="0,0,0,6"/>
                <Setter Property="Height" Value="20"/>
                <Setter Property="Padding" Value="6,1"/>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid Margin="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 面图层选择 -->
            <TextBlock Grid.Row="0" Grid.Column="0" Text="面图层:" VerticalAlignment="Center" Margin="0,0,10,6"/>
            <Grid Grid.Row="0" Grid.Column="1" Margin="0,0,0,6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <ComboBox Grid.Column="0"
                        Style="{StaticResource CompactComboBoxStyle}"
                        ItemsSource="{Binding PolygonLayers}"
                        SelectedItem="{Binding SelectedPolygonLayer}"
                        DisplayMemberPath="Name"/>
                <Button Grid.Column="1" Width="22" Height="22" Margin="5,0,0,0"
                        Style="{StaticResource DefaultButtonStyle}"
                        Command="{Binding RefreshLayersCommand}"
                        ToolTip="刷新图层列表"
                        VerticalAlignment="Center">
                    <TextBlock Text="⟲" FontSize="14" FontWeight="Bold"
                              HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Button>
            </Grid>

            <!-- 使用所选记录 滑动开关（有选择时显示，无选择时隐藏） -->
            <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" Margin="0,0,0,6">
                <StackPanel.Style>
                    <Style TargetType="StackPanel">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Setter Property="Opacity" Value="1"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding HasSelection}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding UseSelection}" Value="False">
                                <Setter Property="Opacity" Value="0.6"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Style>
                <ToggleButton x:Name="UseSelectionSwitch"
                              Style="{StaticResource ToggleSwitchStyle}"
                              IsChecked="{Binding UseSelection, Mode=TwoWay}"
                              VerticalAlignment="Center"/>
                <TextBlock Margin="6,0,0,0" VerticalAlignment="Center" Style="{StaticResource InfoTextBlockStyle}">
                    <Run Text="使用所选记录："/>
                    <Run Text="{Binding SelectedCount}"/>
                </TextBlock>
            </StackPanel>

            <!-- 生成模式 -->
            <TextBlock Grid.Row="2" Grid.Column="0" Text="生成模式:" VerticalAlignment="Center" Margin="0,0,10,6"/>
            <ComboBox Grid.Row="2" Grid.Column="1"
                      Style="{StaticResource CompactComboBoxStyle}"
                      ItemsSource="{Binding GenerationModes}"
                      SelectedItem="{Binding SelectedGenerationMode, Mode=TwoWay}"/>

            <!-- 输出四至点图层 -->
            <TextBlock Grid.Row="3" Grid.Column="0" Text="输出四至点图层:" VerticalAlignment="Center" Margin="0,0,10,6"/>
            <Grid Grid.Row="3" Grid.Column="1" Margin="0,0,0,6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" Text="{Binding OutputPath}" VerticalAlignment="Center"
                        Style="{StaticResource CompactTextBoxStyle}"/>
                <Button Grid.Column="1" Content="浏览..." Height="22" Margin="5,0,0,0" Command="{Binding BrowseOutputCommand}"
                        Style="{StaticResource DefaultButtonStyle}" VerticalAlignment="Center"/>
            </Grid>

            <!-- 保留原始字段 -->
            <TextBlock Grid.Row="4" Grid.Column="0" Text="保留原始字段:" VerticalAlignment="Center" Margin="0,0,10,6"/>
            <Grid Grid.Row="4" Grid.Column="1" Margin="0,0,0,6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" VerticalAlignment="Center" Height="22" Margin="5,0,0,0"
                          Text="{Binding SelectedFieldsDisplayText}"
                          Foreground="{StaticResource TextSecondaryBrush}"/>
                <Button Grid.Column="1" Content="选择字段..." Height="22" Margin="5,0,0,0" Command="{Binding SelectFieldsCommand}"
                        Style="{StaticResource DefaultButtonStyle}" VerticalAlignment="Center"
                        IsEnabled="{Binding HasSelectedLayer}"/>
            </Grid>

            <!-- 进度条 -->
            <ProgressBar Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,0,0,6"
                       Style="{StaticResource ProgressBarStyle}"
                       Value="{Binding Progress}"
                       Minimum="0" Maximum="100"
                       IsIndeterminate="{Binding IsProgressIndeterminate}"
                       Height="6"/>

            <!-- 日志窗口 -->
            <Border Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2"
                   BorderBrush="#CDCDCD" BorderThickness="1"
                   Margin="0,0,0,6">
                <TextBox
                      Style="{StaticResource LogTextBoxStyle}"
                      Text="{Binding LogContent, Mode=OneWay}"
                      BorderThickness="0"
                      VerticalAlignment="Stretch"
                      HorizontalAlignment="Stretch"/>
            </Border>

            <!-- 状态消息和按钮区域 -->
            <Grid Grid.Row="7" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,0,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- 执行状态 -->
                <TextBlock Grid.Row="0" Text="{Binding StatusMessage}" 
                        TextWrapping="Wrap" VerticalAlignment="Center" Margin="0,0,0,6"/>

                <!-- 按钮区域 -->
                <Border Grid.Row="1" BorderBrush="{StaticResource DividerBrush}"
                       BorderThickness="0,1,0,0"
                       Margin="0,3,0,0"
                       Padding="0,8,0,0">
                    <Grid>
                        <Button Content="?" Width="22" Height="22"
                                Style="{StaticResource HelpButtonStyle}"
                                Command="{Binding ShowHelpCommand}"
                                ToolTip="查看工具使用说明"
                                HorizontalAlignment="Left"/>
                        
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <!-- 只在处理时显示停止按钮 -->
                            <Button Content="停止" Width="80" Command="{Binding CancelCommand}" Margin="0,0,10,0"
                                    Style="{StaticResource CancelButtonStyle}"
                                    Visibility="{Binding IsProcessing, Converter={StaticResource BooleanToVisibilityConverter}}"
                                    IsEnabled="{Binding IsProcessing}"/>
                            <Button Content="开始" Width="80" Command="{Binding RunCommand}"
                                    Style="{StaticResource ExecuteButtonStyle}"
                                    IsEnabled="{Binding CanProcess}"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
</UserControl>
