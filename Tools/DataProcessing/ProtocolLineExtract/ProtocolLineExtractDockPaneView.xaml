<UserControl x:Class="XIAOFUTools.Tools.ProtocolLineExtract.ProtocolLineExtractDockPaneView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:XIAOFUTools.Tools.ProtocolLineExtract"
             mc:Ignorable="d" 
             d:DesignHeight="420" d:DesignWidth="400"
             Loaded="UserControl_Loaded">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/XIAOFUTools;component/Styles/ControlStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 面图层选择 -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="输入面图层:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <Grid Grid.Row="0" Grid.Column="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <ComboBox Grid.Column="0"
                      Style="{StaticResource ComboBoxStyle}"
                      ItemsSource="{Binding PolygonLayers}"
                      SelectedItem="{Binding SelectedPolygonLayer}"
                      DisplayMemberPath="Name"/>
            <Button Grid.Column="1" Width="26" Height="26" Margin="5,0,0,0"
                    Style="{StaticResource DefaultButtonStyle}"
                    Command="{Binding RefreshLayersCommand}"
                    ToolTip="刷新图层列表"
                    VerticalAlignment="Center">
                <TextBlock Text="⟲" FontSize="14" FontWeight="Bold"
                          HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Button>
        </Grid>

        <!-- 使用所选记录 滑动开关（有选择时显示，无选择时隐藏） -->
        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" Margin="0,0,0,10">
            <StackPanel.Style>
                <Style TargetType="StackPanel">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Setter Property="Opacity" Value="1"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding HasSelection}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding UseSelection}" Value="False">
                            <Setter Property="Opacity" Value="0.6"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </StackPanel.Style>
            <ToggleButton x:Name="UseSelectionSwitch"
                          Style="{StaticResource ToggleSwitchStyle}"
                          IsChecked="{Binding UseSelection, Mode=TwoWay}"
                          VerticalAlignment="Center"/>
            <TextBlock Margin="6,0,0,0" VerticalAlignment="Center" Style="{StaticResource InfoTextBlockStyle}">
                <Run Text="使用所选记录："/>
                <Run Text="{Binding SelectedCount}"/>
            </TextBlock>
        </StackPanel>

        <!-- 输出路径 -->
        <TextBlock Grid.Row="2" Grid.Column="0" Text="输出:"></TextBlock>
        <Grid Grid.Row="2" Grid.Column="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Text="{Binding OutputPath}" VerticalAlignment="Center" Height="24"
                     Style="{StaticResource TextBoxStyle}"/>
            <Button Grid.Column="1" Content="浏览..." Height="26" Margin="5,0,0,0" Command="{Binding BrowseOutputCommand}"
                    Style="{StaticResource DefaultButtonStyle}" VerticalAlignment="Center"/>
        </Grid>

        <!-- 保留原始字段 -->
        <TextBlock Grid.Row="3" Grid.Column="0" Text="保留原始字段:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <Grid Grid.Row="3" Grid.Column="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" VerticalAlignment="Center" Height="22" Margin="5,0,0,0"
                      Text="{Binding SelectedFieldsDisplayText}"
                      Foreground="{StaticResource TextSecondaryBrush}"/>
            <Button Grid.Column="1" Content="选择字段..." Height="26" Margin="5,0,0,0" Command="{Binding SelectFieldsCommand}"
                    Style="{StaticResource DefaultButtonStyle}" VerticalAlignment="Center"
                    IsEnabled="{Binding HasSelectedLayer}"/>
        </Grid>

        <!-- 选项 -->
        <TextBlock Grid.Row="4" Grid.Column="0" Text="选项:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <StackPanel Grid.Row="4" Grid.Column="1" Orientation="Vertical" Margin="0,0,0,10">
            <CheckBox Content="合并相连线段" IsChecked="{Binding MergeLines}"
                      Style="{StaticResource CheckBoxStyle}"/>
        </StackPanel>

        <!-- 进度条 -->
        <ProgressBar Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2"
                     Style="{StaticResource ProgressBarStyle}"
                     Height="6" Margin="0,0,0,2"
                     Value="{Binding Progress}"
                     Minimum="0" Maximum="100"
                     IsIndeterminate="{Binding IsProgressIndeterminate}"/>

        <!-- 日志窗口 -->
        <Border Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2"
                BorderBrush="#CDCDCD" BorderThickness="1" Margin="0,0,0,10">
            <TextBox Style="{StaticResource LogTextBoxStyle}"
                     Text="{Binding LogContent, Mode=OneWay}"
                     BorderThickness="0" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>
        </Border>

        <!-- 状态与按钮区域 -->
        <Grid Grid.Row="7" Grid.Column="0" Grid.ColumnSpan="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="{Binding StatusMessage}" Margin="0,0,0,10"/>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Content="?" Style="{StaticResource HelpButtonStyle}"
                        Command="{Binding ShowHelpCommand}" ToolTip="显示帮助"/>
                <Button Grid.Column="2" Content="停止" Style="{StaticResource CancelButtonStyle}"
                        Command="{Binding CancelCommand}"/>
                <Button Grid.Column="3" Content="开始" Style="{StaticResource ExecuteButtonStyle}"
                        Command="{Binding RunCommand}" IsEnabled="{Binding CanProcess}"/>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
