<UserControl x:Class="XIAOFUTools.Tools.DataProcessing.ExportDatabaseSchema.ExportDatabaseSchemaDockPaneView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:XIAOFUTools.Tools.DataProcessing.ExportDatabaseSchema"
             mc:Ignorable="d"
             d:DataContext="{Binding Path=ExportDatabaseSchemaViewModel}"
             d:DesignHeight="500" d:DesignWidth="350">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/XIAOFUTools;component/Styles/ControlStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <local:BooleanInverseConverter x:Key="BooleanInverseConverter"/>

            <Style x:Key="FormLabelStyle" TargetType="TextBlock" BasedOn="{StaticResource LabelTextBlockStyle}">
                <Setter Property="Margin" Value="0,3,10,0"/>
                <Setter Property="HorizontalAlignment" Value="Left"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
            </Style>

            <Style x:Key="FormTextBoxStyle" TargetType="TextBox" BasedOn="{StaticResource TextBoxStyle}">
                <Setter Property="Margin" Value="0,2,0,8"/>
                <Setter Property="Height" Value="26"/>
            </Style>

            <Style x:Key="BrowseButtonStyle" TargetType="Button" BasedOn="{StaticResource DefaultButtonStyle}">
                <Setter Property="Width" Value="60"/>
                <Setter Property="Height" Value="26"/>
                <Setter Property="Margin" Value="5,0,0,0"/>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <Border Background="{StaticResource BackgroundBrush}" BorderBrush="#CDCDCD" BorderThickness="0" CornerRadius="0">
        <Grid Margin="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 输入参数 -->
            <GroupBox Grid.Row="0" Header="输入参数"
                     Style="{StaticResource GroupBoxStyle}"
                     Margin="0,0,0,6">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="文件地理数据库 (GDB):"
                              Style="{StaticResource FormLabelStyle}"/>
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0"
                                Style="{StaticResource FormTextBoxStyle}"
                                Text="{Binding InputGdbPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                IsEnabled="{Binding IsProcessing, Converter={StaticResource BooleanInverseConverter}}"
                                ToolTip="选择要导出结构的文件地理数据库"/>
                        <Button Grid.Column="1"
                               Content="浏览"
                               Style="{StaticResource BrowseButtonStyle}"
                               Command="{Binding BrowseInputGdbCommand}"
                               IsEnabled="{Binding IsProcessing, Converter={StaticResource BooleanInverseConverter}}"/>
                    </Grid>
                </Grid>
            </GroupBox>

            <!-- 输出参数 -->
            <GroupBox Grid.Row="1" Header="输出参数"
                     Style="{StaticResource GroupBoxStyle}"
                     Margin="0,0,0,6">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="输出Excel文件:"
                              Style="{StaticResource FormLabelStyle}"/>
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0"
                                Style="{StaticResource FormTextBoxStyle}"
                                Text="{Binding OutputExcelPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                IsEnabled="{Binding IsProcessing, Converter={StaticResource BooleanInverseConverter}}"
                                ToolTip="选择输出的Excel文件路径（支持.xlsx和.xls格式）"/>
                        <Button Grid.Column="1"
                               Content="浏览"
                               Style="{StaticResource BrowseButtonStyle}"
                               Command="{Binding BrowseOutputExcelCommand}"
                               IsEnabled="{Binding IsProcessing, Converter={StaticResource BooleanInverseConverter}}"/>
                    </Grid>
                </Grid>
            </GroupBox>

            <!-- 日志区域 -->
            <GroupBox Grid.Row="2" Header="操作日志"
                     Style="{StaticResource GroupBoxStyle}"
                     Margin="0,0,0,6">
                <TextBox Text="{Binding LogText, Mode=OneWay}"
                        Style="{StaticResource LogTextBoxStyle}"
                        ScrollViewer.VerticalScrollBarVisibility="Auto"
                        ScrollViewer.HorizontalScrollBarVisibility="Auto"/>
            </GroupBox>

            <!-- 底部按钮 -->
            <Grid Grid.Row="3">
                <Border BorderBrush="{StaticResource DividerBrush}"
                       BorderThickness="0,1,0,0"
                       Margin="0,4,0,0"
                       Padding="0,6,0,0">
                    <Grid>
                        <Button x:Name="HelpButton" Content="?"
                                Style="{StaticResource HelpButtonStyle}"
                                Command="{Binding ShowHelpCommand}"
                                ToolTip="查看工具使用说明"
                                HorizontalAlignment="Left"/>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Content="停止"
                                    Style="{StaticResource CancelButtonStyle}"
                                    Command="{Binding StopCommand}"
                                    VerticalAlignment="Center"
                                    Margin="0,0,6,0"/>
                            <Button Content="开始导出"
                                    Style="{StaticResource ExecuteButtonStyle}"
                                    Command="{Binding StartCommand}"
                                    VerticalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Border>
</UserControl>
