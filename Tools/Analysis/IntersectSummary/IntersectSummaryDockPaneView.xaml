<UserControl x:Class="XIAOFUTools.Tools.IntersectSummary.IntersectSummaryDockPaneView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:XIAOFUTools.Tools.IntersectSummary"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="420"
             Loaded="UserControl_Loaded">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/XIAOFUTools;component/Styles/ControlStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- 布尔值转可见性转换器 -->
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

            <!-- 帮助按钮样式 -->
            <Style x:Key="LocalHelpButtonStyle" TargetType="Button" BasedOn="{StaticResource TextButtonStyle}">
                <Setter Property="Width" Value="22"/>
                <Setter Property="Height" Value="22"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Padding" Value="0"/>
                <Setter Property="Margin" Value="0"/>
                <Setter Property="HorizontalAlignment" Value="Left"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 区域红线选择 -->
        <GroupBox Grid.Row="0" Header="区域红线" Style="{StaticResource GroupBoxStyle}" Margin="0,0,0,5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="区域图层:" VerticalAlignment="Center" Margin="0,0,10,5"/>
                <ComboBox Grid.Row="0" Grid.Column="1"
                        Style="{StaticResource ComboBoxStyle}"
                        ItemsSource="{Binding PolygonLayers}"
                        SelectedItem="{Binding SelectedRedlineLayer}"
                        DisplayMemberPath="Name"
                        Margin="0,0,0,5"/>
                <Button Grid.Row="0" Grid.Column="2" Width="22" Height="22" Margin="5,0,0,5"
                        Style="{StaticResource DefaultButtonStyle}"
                        Command="{Binding RefreshLayersCommand}"
                        ToolTip="刷新图层列表"
                        VerticalAlignment="Center">
                    <TextBlock Text="⟲" FontSize="14" FontWeight="Bold"
                              HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Button>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="区域字段(可选):" VerticalAlignment="Top" Margin="0,5,10,0"/>
                <Border Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" 
                        BorderBrush="#ADADAD" BorderThickness="1" CornerRadius="4"
                        MaxHeight="100" Margin="0,0,0,0">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding RegionFields}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <CheckBox Content="{Binding DisplayText}" 
                                              IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                              Margin="5,2"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Border>
            </Grid>
        </GroupBox>

        <!-- 类要素图层选择 -->
        <GroupBox Grid.Row="1" Header="类要素图层" Style="{StaticResource GroupBoxStyle}" Margin="0,0,0,5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="图层:" VerticalAlignment="Center" Margin="0,0,10,5"/>
                <ComboBox Grid.Row="0" Grid.Column="1"
                        Style="{StaticResource ComboBoxStyle}"
                        ItemsSource="{Binding PolygonLayers}"
                        SelectedItem="{Binding SelectedClassLayer}"
                        DisplayMemberPath="Name"
                        Margin="0,0,0,5"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="类字段:" VerticalAlignment="Top" Margin="0,5,10,0"/>
                <Border Grid.Row="1" Grid.Column="1" 
                        BorderBrush="#ADADAD" BorderThickness="1" CornerRadius="4"
                        MaxHeight="100" Margin="0,0,0,0">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding ClassFields}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <CheckBox Content="{Binding DisplayText}" 
                                              IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                              Margin="5,2"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Border>
            </Grid>
        </GroupBox>

        <!-- 输出设置 -->
        <GroupBox Grid.Row="2" Header="输出设置" Style="{StaticResource GroupBoxStyle}" Margin="0,0,0,5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="80"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="输出单位:" VerticalAlignment="Center" Margin="0,0,10,5"/>
                <ComboBox Grid.Row="0" Grid.Column="1"
                        Style="{StaticResource ComboBoxStyle}"
                        ItemsSource="{Binding AreaUnits}"
                        SelectedItem="{Binding SelectedAreaUnit}"
                        Margin="0,0,10,5"/>

                <TextBlock Grid.Row="0" Grid.Column="2" Text="保留位数:" VerticalAlignment="Center" Margin="0,0,10,5"/>
                <TextBox Grid.Row="0" Grid.Column="3"
                       Style="{StaticResource TextBoxStyle}"
                       Text="{Binding DecimalPlaces}"
                       Margin="0,0,0,5"/>
            </Grid>
        </GroupBox>

        <!-- 进度条 -->
        <ProgressBar Grid.Row="3" Margin="0,5,0,5"
                   Style="{StaticResource ProgressBarStyle}"
                   Value="{Binding Progress}"
                   Minimum="0" Maximum="100"
                   IsIndeterminate="{Binding IsProgressIndeterminate}"
                   Height="6"/>

        <!-- 日志窗口（自动填充剩余空间） -->
        <Border Grid.Row="7" BorderBrush="#CDCDCD" BorderThickness="1" Margin="0,0,0,5" MinHeight="60">
            <TextBox Style="{StaticResource LogTextBoxStyle}"
                  Text="{Binding LogContent, Mode=OneWay}"
                  BorderThickness="0"
                  VerticalAlignment="Stretch"
                  HorizontalAlignment="Stretch"
                  IsReadOnly="True"
                  TextWrapping="Wrap"
                  VerticalScrollBarVisibility="Auto"/>
        </Border>

        <!-- 按钮区域 -->
        <Border Grid.Row="8" BorderBrush="{StaticResource DividerBrush}"
               BorderThickness="0,1,0,0"
               Margin="0,8,0,0"
               Padding="0,8,0,0">
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <Button Content="?" Width="22" Height="22"
                            Style="{StaticResource LocalHelpButtonStyle}"
                            Command="{Binding ShowHelpCommand}"
                            ToolTip="查看工具使用说明"/>
                    <Button Content="查看结果" Height="22" Margin="8,0,0,0"
                            Style="{StaticResource DefaultButtonStyle}"
                            Command="{Binding ShowResultCommand}"
                            Visibility="{Binding HasResult, Converter={StaticResource BooleanToVisibilityConverter}}"
                            ToolTip="点击查看详细汇总结果表格"/>
                    <Button Content="导出" Height="22" Margin="8,0,0,0"
                            Style="{StaticResource DefaultButtonStyle}"
                            Command="{Binding ExportCommand}"
                            Visibility="{Binding HasResult, Converter={StaticResource BooleanToVisibilityConverter}}"
                            ToolTip="导出汇总结果到Excel"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="停止"
                            Style="{StaticResource CancelButtonStyle}"
                            Command="{Binding CancelCommand}"
                            IsEnabled="{Binding IsProcessing}"/>
                    <Button Content="开始"
                            Style="{StaticResource ExecuteButtonStyle}"
                            Command="{Binding RunCommand}"
                            IsEnabled="{Binding CanProcess}"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
