<UserControl x:Class="XIAOFUTools.Tools.DocumentBatchReplace.DocumentBatchReplaceDockPaneView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="420">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/XIAOFUTools;component/Styles/ControlStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel>
                <GroupBox Header="文件列表" Style="{StaticResource GroupBoxStyle}" Margin="0,0,0,10">
                    <Grid Margin="8">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                            <Button Content="添加文件" Command="{Binding AddFilesCommand}" Style="{StaticResource DefaultButtonStyle}" Margin="0,0,6,0"/>
                            <Button Content="添加文件夹" Command="{Binding AddFolderCommand}" Style="{StaticResource DefaultButtonStyle}" Margin="0,0,6,0"/>
                            <Button Content="移除所选" Command="{Binding RemoveSelectedFileCommand}" Style="{StaticResource CancelButtonStyle}" Margin="0,0,6,0"/>
                            <Button Content="清空文件" Command="{Binding ClearFilesCommand}" Style="{StaticResource CancelButtonStyle}"/>
                        </StackPanel>

                        <Border Grid.Row="1" BorderBrush="#CDCDCD" BorderThickness="1" Height="160" MaxHeight="220">
                            <ListBox ItemsSource="{Binding InputFiles}"
                                     SelectedItem="{Binding SelectedInputFile, Mode=TwoWay}"
                                     Style="{StaticResource ListBoxStyle}"
                                     AllowDrop="True"
                                     DragOver="DropArea_DragOver"
                                     Drop="DropArea_Drop"/>
                        </Border>

                        <TextBlock Grid.Row="2" Text="{Binding FileCountText}" Foreground="#666666" Margin="0,6,0,0"/>
                    </Grid>
                </GroupBox>

                <GroupBox Header="替换规则" Style="{StaticResource GroupBoxStyle}" Margin="0,0,0,10">
                    <Grid Margin="8">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <WrapPanel Grid.Row="0" Margin="0,0,0,8">
                            <CheckBox Content="区分大小写" IsChecked="{Binding MatchCase}" Style="{StaticResource CheckBoxStyle}" Margin="0,0,10,0"/>
                            <CheckBox Content="全字匹配" IsChecked="{Binding MatchWholeWord}" Style="{StaticResource CheckBoxStyle}" Margin="0,0,10,0"/>
                            <CheckBox Content="区分全/半角" IsChecked="{Binding MatchByte}" Style="{StaticResource CheckBoxStyle}" Margin="0,0,10,0"/>
                            <CheckBox Content="使用通配符" IsChecked="{Binding UseWildcards}" Style="{StaticResource CheckBoxStyle}"/>
                        </WrapPanel>

                        <DataGrid Grid.Row="1"
                                  ItemsSource="{Binding ReplaceRules}"
                                  AutoGenerateColumns="False"
                                  HeadersVisibility="Column"
                                  CanUserAddRows="False"
                                  CanUserDeleteRows="False"
                                  SelectionMode="Single"
                                  Style="{StaticResource DataGridStyle}"
                                  MinHeight="120"
                                  MaxHeight="200">
                            <DataGrid.Columns>
                                <DataGridTemplateColumn Header="查找" Width="*">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding FindText}"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                    <DataGridTemplateColumn.CellEditingTemplate>
                                        <DataTemplate>
                                            <TextBox Text="{Binding FindText, UpdateSourceTrigger=PropertyChanged}" MaxLength="100" Style="{StaticResource TextBoxStyle}"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellEditingTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTemplateColumn Header="替换为" Width="*">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding ReplaceText}"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                    <DataGridTemplateColumn.CellEditingTemplate>
                                        <DataTemplate>
                                            <TextBox Text="{Binding ReplaceText, UpdateSourceTrigger=PropertyChanged}" MaxLength="100" Style="{StaticResource TextBoxStyle}"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellEditingTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTemplateColumn Header="操作" Width="60">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="删除"
                                                    Width="48"
                                                    Height="26"
                                                    Style="{StaticResource CancelButtonStyle}"
                                                    Command="{Binding DataContext.RemoveRuleCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                    CommandParameter="{Binding}"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>

                        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                            <Button Content="添加规则" Command="{Binding AddRuleCommand}" Style="{StaticResource DefaultButtonStyle}" Margin="0,0,6,0"/>
                            <Button Content="清空规则" Command="{Binding ClearRulesCommand}" Style="{StaticResource CancelButtonStyle}"/>
                        </StackPanel>
                    </Grid>
                </GroupBox>

                <GroupBox Header="保存方式" Style="{StaticResource GroupBoxStyle}">
                    <Grid Margin="8">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                            <RadioButton Content="另存为副本" IsChecked="{Binding SaveAsCopy}" GroupName="SaveMode" Style="{StaticResource RadioButtonStyle}" Margin="0,0,12,0"/>
                            <RadioButton Content="覆盖原文件" IsChecked="{Binding OverwriteOriginal}" GroupName="SaveMode" Style="{StaticResource RadioButtonStyle}"/>
                        </StackPanel>

                        <CheckBox Grid.Row="1" Content="遍历子文件夹（添加文件夹时生效）" IsChecked="{Binding TraverseSubfolders}" Style="{StaticResource CheckBoxStyle}" Margin="0,0,0,8"/>

                        <Grid Grid.Row="2" Visibility="{Binding ShowOutputFolder, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Grid.Column="0" Text="{Binding OutputFolder, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TextBoxStyle}" ToolTip="选择副本输出目录"/>
                            <Button Grid.Column="1" Content="浏览" Command="{Binding SelectOutputFolderCommand}" Style="{StaticResource DefaultButtonStyle}" Margin="6,0,0,0" Width="60"/>
                        </Grid>
                    </Grid>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>

        <Grid Grid.Row="1" Margin="0,8,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <ProgressBar Grid.Row="0"
                         Value="{Binding Progress}"
                         IsIndeterminate="{Binding IsProgressIndeterminate}"
                         Style="{StaticResource ProgressBarStyle}"
                         Height="6"
                         Margin="0,0,0,5"/>

            <Border Grid.Row="1" BorderBrush="#CDCDCD" BorderThickness="1" Height="110" MaxHeight="140">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                    <TextBox Text="{Binding LogText}"
                             Style="{StaticResource LogTextBoxStyle}"
                             IsReadOnly="True"
                             TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Disabled"
                             HorizontalScrollBarVisibility="Disabled"/>
                </ScrollViewer>
            </Border>
        </Grid>

        <TextBlock Grid.Row="2" Text="{Binding StatusText}" Margin="0,5,0,5" FontSize="11" Foreground="#666666"/>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="?" Command="{Binding HelpCommand}" Style="{StaticResource HelpButtonStyle}" Width="22" Height="22" ToolTip="显示帮助信息"/>
            <Button Grid.Column="2" Content="停止" Command="{Binding StopReplaceCommand}" Style="{StaticResource CancelButtonStyle}" Width="60" Height="26" Margin="5,0"/>
            <Button Grid.Column="3" Content="开始替换" Command="{Binding StartReplaceCommand}" Style="{StaticResource ExecuteButtonStyle}" Width="80" Height="26"/>
        </Grid>
    </Grid>
</UserControl>
