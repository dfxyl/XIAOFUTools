<Window x:Class="XIAOFUTools.Tools.FeatureToTxt.HeaderConfigDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="头部信息配置管理" 
        Height="550" Width="850"
        MinHeight="450" MinWidth="700"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="CanResize"
        Background="White">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/XIAOFUTools;component/Styles/ControlStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 主内容区域 -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="240"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 左侧：配置列表 -->
            <Border Grid.Column="0" BorderBrush="#CDCDCD" BorderThickness="1" Background="#F9F9F9" Margin="0,0,12,0">
                <Grid Margin="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- 标题 -->
                    <TextBlock Grid.Row="0" Text="配置列表" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>

                    <!-- 配置列表 -->
                    <Border Grid.Row="1" BorderBrush="#CDCDCD" BorderThickness="1" Background="White" Margin="0,0,0,10">
                        <ListBox x:Name="ConfigListBox"
                                 ItemsSource="{Binding Configs}"
                                 SelectedItem="{Binding SelectedConfig, Mode=TwoWay}"
                                 DisplayMemberPath="Name"
                                 Background="Transparent" 
                                 BorderThickness="0"
                                 Padding="4"/>
                    </Border>

                    <!-- 列表操作按钮 -->
                    <StackPanel Grid.Row="2" Orientation="Vertical">
                        <Button Content="➕ 新增配置" 
                                Command="{Binding AddConfigCommand}" 
                                Style="{StaticResource DefaultButtonStyle}" 
                                Margin="0,0,0,8"
                                Height="34"
                                FontSize="12"/>
                        <Button Content="➖ 删除配置" 
                                Command="{Binding DeleteConfigCommand}" 
                                Style="{StaticResource CancelButtonStyle}"
                                Height="34"
                                FontSize="12"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- 右侧：配置编辑 -->
            <Border Grid.Column="1" BorderBrush="#CDCDCD" BorderThickness="1" Background="White">
                <Grid Margin="16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- 标题 -->
                    <TextBlock Grid.Row="0" Text="配置详情" FontWeight="Bold" FontSize="14" Margin="0,0,0,14"/>

                    <!-- 配置名称 -->
                    <Grid Grid.Row="1" Margin="0,0,0,14">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBlock Grid.Column="0" 
                                   Text="配置名称:" 
                                   VerticalAlignment="Center"
                                   FontWeight="SemiBold"
                                   Style="{StaticResource FormLabelStyle}" 
                                   Margin="0,0,12,0"/>
                        <TextBox Grid.Column="1" 
                                 Text="{Binding SelectedConfig.Name, UpdateSourceTrigger=PropertyChanged}" 
                                 Style="{StaticResource TextBoxStyle}"
                                 Height="28"
                                 VerticalContentAlignment="Center"/>
                    </Grid>

                    <!-- 头部信息文本 -->
                    <Grid Grid.Row="2">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <StackPanel Grid.Row="0" Margin="0,0,0,8">
                            <TextBlock Text="头部信息内容" FontWeight="SemiBold" FontSize="12" Margin="0,0,0,4"/>
                            <TextBlock Text="直接粘贴完整的头部信息文本，包括 [属性描述] 和 [地块坐标] 等标记" 
                                       Foreground="#666666" 
                                       FontSize="11"
                                       TextWrapping="Wrap"/>
                        </StackPanel>
                        
                        <Border Grid.Row="1" 
                                BorderBrush="#CDCDCD" 
                                BorderThickness="1" 
                                Background="#FAFAFA">
                            <TextBox Text="{Binding SelectedConfig.HeaderText, UpdateSourceTrigger=PropertyChanged}" 
                                     AcceptsReturn="True"
                                     TextWrapping="NoWrap"
                                     VerticalScrollBarVisibility="Auto"
                                     HorizontalScrollBarVisibility="Auto"
                                     FontFamily="Consolas"
                                     FontSize="11"
                                     Padding="10"
                                     Background="Transparent"
                                     BorderThickness="0"/>
                        </Border>
                    </Grid>
                </Grid>
            </Border>
        </Grid>

        <!-- 底部按钮 -->
        <StackPanel Grid.Row="1" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="0,15,0,0">
            <Button Content="保存并应用" 
                    Width="100" 
                    Height="32"
                    Margin="0,0,8,0" 
                    Style="{StaticResource ExecuteButtonStyle}" 
                    Click="SaveButton_Click"/>
            <Button Content="取消" 
                    Width="80" 
                    Height="32"
                    Style="{StaticResource CancelButtonStyle}" 
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
