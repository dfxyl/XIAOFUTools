<UserControl x:Class="XIAOFUTools.Tools.ImagesToPdf.ImagesToPdfDockPaneView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="350">
    
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/XIAOFUTools;component/Styles/ControlStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 参数设置区域 -->
        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel>
                <!-- 输入文件夹 -->
                <TextBlock Text="输入文件夹:" FontWeight="Bold" Margin="0,0,0,5"/>
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" Text="{Binding InputFolder, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                             Style="{StaticResource TextBoxStyle}" 
                             IsReadOnly="False" ToolTip="选择包含图片文件的输入文件夹"/>
                    <Button Grid.Column="1" Content="浏览" 
                            Command="{Binding SelectInputFolderCommand}"
                            Style="{StaticResource DefaultButtonStyle}"
                            Width="60" Height="22" Margin="5,0,0,0"/>
                </Grid>

                <!-- 保存在源路径 -->
                <CheckBox Content="保存在源路径"
                          IsChecked="{Binding SaveToSourcePath}"
                          Style="{StaticResource CheckBoxStyle}"
                          ToolTip="将转换后的PDF保存在图片所在的路径"
                          Margin="0,0,0,10"/>

                <!-- 输出文件夹 -->
                <StackPanel Visibility="{Binding ShowOutputFolder, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <TextBlock Text="输出文件夹:" FontWeight="Bold" Margin="0,0,0,5"/>
                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Text="{Binding OutputFolder, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                 Style="{StaticResource TextBoxStyle}" 
                                 IsReadOnly="False" ToolTip="选择输出文件夹"/>
                        <Button Grid.Column="1" Content="浏览" 
                                Command="{Binding SelectOutputFolderCommand}"
                                Style="{StaticResource DefaultButtonStyle}"
                                Width="60" Height="22" Margin="5,0,0,0"/>
                    </Grid>
                </StackPanel>

                <!-- 选项设置 -->
                <TextBlock Text="选项设置:" FontWeight="Bold" Margin="0,0,0,5"/>
                <StackPanel Margin="0,0,0,10">
                    <!-- 按原结构输出 -->
                    <CheckBox Content="按原结构输出"
                              IsChecked="{Binding KeepOriginalStructure}"
                              Style="{StaticResource CheckBoxStyle}"
                              ToolTip="保持输入文件夹的目录结构"
                              Visibility="{Binding ShowKeepOriginalStructure, Converter={StaticResource BooleanToVisibilityConverter}}"
                              Margin="0,3"/>
                    
                    <!-- 是否合并 -->
                    <CheckBox Content="合并子文件夹图片"
                              IsChecked="{Binding MergeImages}"
                              Style="{StaticResource CheckBoxStyle}"
                              ToolTip="将每个子文件夹中的所有图片合并为一个PDF文件"
                              Margin="0,3"/>
                    
                    <!-- 遍历子文件夹 -->
                    <CheckBox Content="遍历子文件夹"
                              IsChecked="{Binding TraverseSubfolders}"
                              Style="{StaticResource CheckBoxStyle}"
                              ToolTip="处理所有子文件夹中的图片文件"
                              Margin="0,3"/>
                </StackPanel>

                <!-- DPI设置 -->
                <TextBlock Text="DPI设置:" FontWeight="Bold" Margin="0,10,0,5"/>
                <TextBox Text="{Binding Dpi, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                         Style="{StaticResource TextBoxStyle}" 
                         ToolTip="控制PDF页面物理尺寸。300 DPI为印刷标准，图片尺寸合理；72 DPI为屏幕标准，图片尺寸较大"
                         Margin="0,0,0,10"/>

            </StackPanel>
        </ScrollViewer>

        <!-- 进度和日志区域 -->
        <Grid Grid.Row="1" Margin="0,10,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 进度条 -->
            <ProgressBar Grid.Row="0" 
                         Value="{Binding Progress}" 
                         IsIndeterminate="{Binding IsProgressIndeterminate}"
                         Style="{StaticResource ProgressBarStyle}"
                         Height="6" Margin="0,0,0,5"/>

            <!-- 日志窗口 -->
            <Border Grid.Row="1" BorderBrush="#CDCDCD" BorderThickness="1">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                    <TextBox Text="{Binding LogText}" 
                             Style="{StaticResource LogTextBoxStyle}"
                             IsReadOnly="True" 
                             TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Disabled"
                             HorizontalScrollBarVisibility="Disabled"/>
                </ScrollViewer>
            </Border>
        </Grid>

        <!-- 状态信息 -->
        <TextBlock Grid.Row="2" Text="{Binding StatusText}" 
                   Margin="0,5,0,5" FontSize="11" 
                   Foreground="#666666"/>

        <!-- 按钮区域 -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- 帮助按钮 -->
            <Button Grid.Column="0" Content="?" 
                    Command="{Binding HelpCommand}"
                    Style="{StaticResource HelpButtonStyle}"
                    Width="22" Height="22" 
                    ToolTip="显示帮助信息"/>

            <!-- 停止按钮 -->
            <Button Grid.Column="2" Content="停止" 
                    Command="{Binding StopConversionCommand}"
                    Style="{StaticResource CancelButtonStyle}"
                    Width="60" Height="22" Margin="5,0"/>

            <!-- 开始按钮 -->
            <Button Grid.Column="3" Content="开始" 
                    Command="{Binding StartConversionCommand}"
                    Style="{StaticResource ExecuteButtonStyle}"
                    Width="60" Height="22"/>
        </Grid>
    </Grid>
</UserControl>
