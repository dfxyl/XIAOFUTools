<UserControl x:Class="XIAOFUTools.Tools.ExportToKml.ExportToKmlDockPaneView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:XIAOFUTools.Tools.ExportToKml"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="400"
             Loaded="UserControl_Loaded">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/XIAOFUTools;component/Styles/ControlStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- 布尔值转可见性转换器 -->
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- 输入图层选择 -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="输入图层:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <Grid Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <ComboBox Grid.Column="0"
                    Style="{StaticResource ComboBoxStyle}"
                    ItemsSource="{Binding FeatureLayers}"
                    SelectedItem="{Binding SelectedInputLayer}"
                    DisplayMemberPath="Name"/>
            <Button Grid.Column="1" Width="26" Height="26" Margin="5,0,0,0"
                    Style="{StaticResource DefaultButtonStyle}"
                    Command="{Binding RefreshLayersCommand}"
                    ToolTip="刷新图层列表"
                    VerticalAlignment="Center">
                <TextBlock Text="⟲" FontSize="14" FontWeight="Bold"
                          HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Button>
        </Grid>

        <!-- 分组字段选择 -->
        <TextBlock Grid.Row="1" Grid.Column="0" Text="分组字段:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <ComboBox Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" Margin="0,0,0,10"
                Style="{StaticResource ComboBoxStyle}"
                ItemsSource="{Binding GroupFields}"
                SelectedItem="{Binding SelectedGroupField}"
                ToolTip="选择分组字段，选择[不分组]则直接导出全部要素"/>

        <!-- 导出格式选择 -->
        <TextBlock Grid.Row="2" Grid.Column="0" Text="导出格式:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <ComboBox Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2" Margin="0,0,0,10"
                Style="{StaticResource ComboBoxStyle}"
                ItemsSource="{Binding ExportFormats}"
                SelectedItem="{Binding SelectedExportFormat}"/>

        <!-- 输出文件夹 -->
        <TextBlock Grid.Row="3" Grid.Column="0" Text="输出文件夹:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <TextBox Grid.Row="3" Grid.Column="1" Margin="0,0,5,10"
               Style="{StaticResource TextBoxStyle}"
               Text="{Binding OutputFolder}"
               ToolTip="选择导出KML/KMZ文件的输出文件夹"/>
        <Button Grid.Row="3" Grid.Column="2" Content="浏览" Width="60" Margin="0,0,0,10"
                Style="{StaticResource DefaultButtonStyle}"
                Command="{Binding BrowseOutputFolderCommand}"
                ToolTip="选择输出文件夹"/>

        <!-- 标注设置 - 复选框单独一行 -->
        <CheckBox Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="3" Margin="0,0,0,10"
                  Content="生成文字标注层"
                  IsChecked="{Binding EnableLabel}"
                  ToolTip="勾选后将根据选择的字段生成独立的文字标注"/>
        
        <!-- 标注字段选择（仅在勾选时显示） - 单独一行 -->
        <TextBlock Grid.Row="5" Grid.Column="0" Text="标注字段:" VerticalAlignment="Center" Margin="0,0,10,10"
                   Visibility="{Binding ShowLabelField, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        <ComboBox Grid.Row="5" Grid.Column="1" Grid.ColumnSpan="2" Margin="0,0,0,10"
                Height="24"
                ItemsSource="{Binding LabelFields}"
                SelectedItem="{Binding SelectedLabelField}"
                Visibility="{Binding ShowLabelField, Converter={StaticResource BooleanToVisibilityConverter}}"
                ToolTip="选择用于标注显示的字段"/>

        <!-- 进度条 -->
        <ProgressBar Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="3" Margin="0,0,0,10"
                   Style="{StaticResource ProgressBarStyle}"
                   Value="{Binding Progress}"
                   Minimum="0" Maximum="100"
                   IsIndeterminate="{Binding IsProgressIndeterminate}"
                   Height="6"/>

        <!-- 日志窗口 -->
        <Border Grid.Row="7" Grid.Column="0" Grid.ColumnSpan="3"
               BorderBrush="#CDCDCD" BorderThickness="1"
               Margin="0,0,0,10"
               MinHeight="120">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                <TextBox
                      Style="{StaticResource LogTextBoxStyle}"
                      Text="{Binding LogContent, Mode=OneWay}"
                      BorderThickness="0"
                      VerticalAlignment="Stretch"
                      HorizontalAlignment="Stretch"
                      TextWrapping="Wrap"
                      AcceptsReturn="True"
                      IsReadOnly="True"
                      Background="Transparent"/>
            </ScrollViewer>
        </Border>

        <!-- 状态消息和按钮区域 -->
        <Grid Grid.Row="8" Grid.Column="0" Grid.ColumnSpan="3" Margin="0,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- 执行状态 -->
            <TextBlock Grid.Row="0" Text="{Binding StatusMessage}" 
                    TextWrapping="Wrap" VerticalAlignment="Center" Margin="0,0,0,10"/>

            <!-- 按钮区域 -->
            <Border Grid.Row="1" BorderBrush="{StaticResource DividerBrush}"
                   BorderThickness="0,1,0,0"
                   Margin="0,3,0,0"
                   Padding="0,8,0,0">
                <Grid>
                    <Button Content="?" Width="22" Height="22"
                            Style="{StaticResource HelpButtonStyle}"
                            Command="{Binding ShowHelpCommand}"
                            ToolTip="查看工具使用说明"
                            HorizontalAlignment="Left"/>
                    
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <!-- 只在处理时显示停止按钮 -->
                        <Button Content="停止" Width="80" Command="{Binding CancelCommand}" Margin="0,0,10,0"
                                Style="{StaticResource CancelButtonStyle}"
                                Visibility="{Binding IsProcessing, Converter={StaticResource BooleanToVisibilityConverter}}"
                                IsEnabled="{Binding IsProcessing}"/>
                        <Button Content="导出" Width="80" Command="{Binding RunCommand}"
                                Style="{StaticResource ExecuteButtonStyle}"
                                IsEnabled="{Binding CanProcess}"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
