<UserControl x:Class="XIAOFUTools.Tools.SpecialCoordinateTransform.SpecialCoordinateTransformDockPaneView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:extensions="clr-namespace:ArcGIS.Desktop.Extensions;assembly=ArcGIS.Desktop.Extensions"
             xmlns:local="clr-namespace:XIAOFUTools.Tools.SpecialCoordinateTransform"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="400"
             d:DataContext="{Binding Path=SpecialCoordinateTransformDockPaneViewModel}"
             Loaded="UserControl_Loaded">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/XIAOFUTools;component/Styles/ControlStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- 布尔值转可见性转换器 -->
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- 输入图层选择 -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="输入图层:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <Grid Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <ComboBox Grid.Column="0"
                    Style="{StaticResource ComboBoxStyle}"
                    ItemsSource="{Binding FeatureLayers}"
                    SelectedItem="{Binding SelectedInputLayer}"
                    DisplayMemberPath="Name"/>
            <Button Grid.Column="1" Width="22" Height="22" Margin="5,0,0,0"
                    Style="{StaticResource DefaultButtonStyle}"
                    Command="{Binding RefreshLayersCommand}"
                    ToolTip="刷新图层列表"
                    VerticalAlignment="Center">
                <TextBlock Text="⟲" FontSize="14" FontWeight="Bold"
                          HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Button>
        </Grid>

        <!-- 输出路径 -->
        <TextBlock Grid.Row="1" Grid.Column="0" Text="输出要素类:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <TextBox Grid.Row="1" Grid.Column="1" Margin="0,0,5,10"
               Style="{StaticResource TextBoxStyle}"
               Text="{Binding OutputPath}"
               ToolTip="默认输出到当前项目的默认地理数据库，命名格式：要素名称_转换坐标名称&#x0A;点击浏览选择：.gdb文件夹→数据库要素类，普通文件夹→Shapefile"/>
        <Button Grid.Row="1" Grid.Column="2" Content="浏览" Width="60" Margin="0,0,0,10"
                Style="{StaticResource DefaultButtonStyle}"
                Command="{Binding BrowseOutputPathCommand}"
                ToolTip="选择输出位置：.gdb数据库文件夹 或 普通文件夹"/>

        <!-- 转换类型选择 -->
        <TextBlock Grid.Row="2" Grid.Column="0" Text="转换类型:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <ComboBox Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2" Margin="0,0,0,10"
                Style="{StaticResource ComboBoxStyle}"
                ItemsSource="{Binding ConversionTypes}"
                SelectedItem="{Binding SelectedConversionType}"
                DisplayMemberPath="DisplayName"/>

        <!-- 进度条 -->
        <ProgressBar Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="3" Margin="0,0,0,10"
                   Style="{StaticResource ProgressBarStyle}"
                   Value="{Binding Progress}"
                   Minimum="0" Maximum="100"
                   IsIndeterminate="{Binding IsProgressIndeterminate}"
                   Height="6"/>

        <!-- 日志窗口 -->
        <Border Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="3"
               BorderBrush="#CDCDCD" BorderThickness="1"
               Margin="0,0,0,10"
               MinHeight="120">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                <TextBox
                      Style="{StaticResource LogTextBoxStyle}"
                      Text="{Binding LogContent, Mode=OneWay}"
                      BorderThickness="0"
                      VerticalAlignment="Stretch"
                      HorizontalAlignment="Stretch"
                      TextWrapping="Wrap"
                      AcceptsReturn="True"
                      IsReadOnly="True"
                      Background="Transparent"/>
            </ScrollViewer>
        </Border>

        <!-- 状态消息和按钮区域 -->
        <Grid Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="3" Margin="0,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- 执行状态 -->
            <TextBlock Grid.Row="0" Text="{Binding StatusMessage}" 
                    TextWrapping="Wrap" VerticalAlignment="Center" Margin="0,0,0,10"/>

            <!-- 按钮区域 -->
            <Border Grid.Row="1" BorderBrush="{StaticResource DividerBrush}"
                   BorderThickness="0,1,0,0"
                   Margin="0,3,0,0"
                   Padding="0,8,0,0">
                <Grid>
                    <Button Content="?" Width="22" Height="22"
                            Style="{StaticResource HelpButtonStyle}"
                            Command="{Binding ShowHelpCommand}"
                            ToolTip="查看工具使用说明"
                            HorizontalAlignment="Left"/>
                    
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <!-- 只在处理时显示停止按钮 -->
                        <Button Content="停止" Width="80" Command="{Binding CancelCommand}" Margin="0,0,10,0"
                                Style="{StaticResource CancelButtonStyle}"
                                Visibility="{Binding IsProcessing, Converter={StaticResource BooleanToVisibilityConverter}}"
                                IsEnabled="{Binding IsProcessing}"/>
                        <Button Content="开始" Width="80" Command="{Binding RunCommand}"
                                Style="{StaticResource ExecuteButtonStyle}"
                                IsEnabled="{Binding CanProcess}"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
