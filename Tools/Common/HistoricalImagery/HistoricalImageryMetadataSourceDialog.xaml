<Window x:Class="XIAOFUTools.Tools.HistoricalImagery.HistoricalImageryMetadataSourceDialog"
        x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="选择影像信息查询来源"
        Width="460"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/XIAOFUTools;component/Styles/ControlStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="检测到多个可用来源，请选择要查询的影像版本。"
                   TextWrapping="Wrap"
                   Margin="0,0,0,10"
                   Foreground="{StaticResource TextPrimaryBrush}"/>

        <TextBlock Grid.Row="1"
                   Text="可选来源"
                   Margin="0,0,0,6"
                   FontWeight="SemiBold"/>

        <ListBox x:Name="CandidatesListBox"
                 Grid.Row="2"
                 MinHeight="180"
                 MaxHeight="280"
                 BorderBrush="{StaticResource DividerBrush}"
                 BorderThickness="1"
                 SelectedIndex="0">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Padding="8" BorderThickness="0,0,0,1" BorderBrush="{StaticResource DividerBrush}">
                        <StackPanel>
                            <TextBlock Text="{Binding DisplayText}"
                                       FontWeight="SemiBold"
                                       TextWrapping="Wrap"/>
                            <TextBlock Text="{Binding Version.Summary}"
                                       Margin="0,2,0,0"
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       FontSize="11"
                                       TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">
            <Button Content="取消"
                    Width="80"
                    Margin="0,0,8,0"
                    Style="{StaticResource CancelButtonStyle}"
                    Click="CancelButton_Click"/>
            <Button Content="查询"
                    Width="80"
                    Style="{StaticResource ExecuteButtonStyle}"
                    Click="ConfirmButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
