<Window x:Class="XIAOFUTools.Tools.About.AboutDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="关于 XIAOFU工具箱" 
        Height="500" Width="450"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False"
        WindowStyle="SingleBorderWindow">
    
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/XIAOFUTools;component/Styles/ControlStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            
            <!-- 标题样式 -->
            <Style x:Key="TitleTextStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="20"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Foreground" Value="#2c3e50"/>
                <Setter Property="HorizontalAlignment" Value="Center"/>
                <Setter Property="Margin" Value="0,10"/>
            </Style>
            
            <!-- 版本信息样式 -->
            <Style x:Key="VersionTextStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Foreground" Value="#3498db"/>
                <Setter Property="HorizontalAlignment" Value="Center"/>
                <Setter Property="Margin" Value="0,5"/>
            </Style>
            
            <!-- 信息标签样式 -->
            <Style x:Key="InfoLabelStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Foreground" Value="#34495e"/>
                <Setter Property="Margin" Value="0,6,0,2"/>
            </Style>
            
            <!-- 信息内容样式 -->
            <Style x:Key="InfoContentStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="11"/>
                <Setter Property="Foreground" Value="#7f8c8d"/>
                <Setter Property="TextWrapping" Value="Wrap"/>
                <Setter Property="LineHeight" Value="16"/>
            </Style>
            
            <!-- 警告文本样式 -->
            <Style x:Key="WarningTextStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="11"/>
                <Setter Property="Foreground" Value="#e74c3c"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="TextWrapping" Value="Wrap"/>
                <Setter Property="LineHeight" Value="16"/>
                <Setter Property="Margin" Value="0,5"/>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 头部区域 -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 图标 -->
            <Image Grid.Column="0"
                   Source="pack://application:,,,/XIAOFUTools;component/Images/Toolbox.png"
                   Width="32" Height="32"
                   VerticalAlignment="Center"
                   Margin="0,0,12,0"/>

            <!-- 标题和版本信息 -->
            <StackPanel Grid.Column="1" Orientation="Vertical" VerticalAlignment="Center">
                <TextBlock Text="XIAOFU工具箱"
                          FontSize="16" FontWeight="Bold"
                          Foreground="#2c3e50"
                          Margin="0,0,0,2"/>
                <TextBlock x:Name="VersionText"
                          Text="版本 1.2.5"
                          FontSize="12" FontWeight="SemiBold"
                          Foreground="#3498db"/>
            </StackPanel>
        </Grid>

        <!-- 主要内容区域 -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,12">
            <StackPanel>
                <!-- 特别提醒 -->
                <TextBlock Text="特别提醒:" Style="{StaticResource InfoLabelStyle}"/>
                <TextBlock x:Name="SpecialNoticeText" 
                          Text="在使用任何工具前，请务必提前对原始数据进行完整备份，以避免因误操作或不可预见的运行结果导致数据丢失或损坏，造成不必要的损失！建议在备份数据基础上进行操作，并确认运行环境符合工具要求。" 
                          Style="{StaticResource WarningTextStyle}"/>
                <!-- 当前版本号 -->
                <TextBlock Text="当前版本号:" Style="{StaticResource InfoLabelStyle}"/>
                <TextBlock x:Name="CurrentVersionText" Text="1.2.5" Style="{StaticResource InfoContentStyle}"/>

                <!-- 开发环境 -->
                <TextBlock Text="开发环境:" Style="{StaticResource InfoLabelStyle}"/>
                <TextBlock x:Name="DevelopmentEnvironmentText"
                          Text="ArcGIS Pro 3.6+ / .NET 8.0 / Visual Studio 2022"
                          Style="{StaticResource InfoContentStyle}"/>

                <!-- 版本更新信息 -->
                <TextBlock Text="版本更新信息:" Style="{StaticResource InfoLabelStyle}"/>
                <TextBlock x:Name="UpdateInfoText"
                          Text="v1.2.5 更新：&#x0a;• AI助手完成架构重构，新增图层查询/缓冲/裁剪/压盖汇总等智能GIS工具链&#x0a;• 分析、转换、数据处理、编辑等多模块功能与UI交互优化&#x0a;• 修复若干bug"
                          Style="{StaticResource InfoContentStyle}"/>

                <!-- 作者信息 -->
                <TextBlock Text="作者信息:" Style="{StaticResource InfoLabelStyle}"/>
                <TextBlock x:Name="AuthorInfoText"
                          Text="作者: XIAOFU&#x0a;哔哩哔哩: XIAOFUGIS&#x0a;QQ群: 967758553&#x0a;联系方式: WX（fu76488）/ QQ（1922759464）"
                          Style="{StaticResource InfoContentStyle}"/>
                <!-- 扫码反馈 -->
                <TextBlock Text="扫码反馈:" Style="{StaticResource InfoLabelStyle}"/>
                <Grid Margin="0,4,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- 反馈表 -->
                    <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                        <Image x:Name="FeedbackQrImage" Source="pack://application:,,,/XIAOFUTools;component/Images/反馈表二维码.png"
                               Width="150" Stretch="Uniform" Margin="0,0,0,4"
                               Cursor="Hand" ToolTip="点击放大查看"
                               MouseLeftButtonUp="QrImage_Click"/>
                        <TextBlock Text="反馈表（扫码填写）" HorizontalAlignment="Center" Style="{StaticResource InfoContentStyle}"/>
                    </StackPanel>

                    <!-- 新增工具收集清单 -->
                    <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                        <Image x:Name="ToolRequestQrImage" Source="pack://application:,,,/XIAOFUTools;component/Images/新增工具收集清单二维码.png"
                               Width="150" Stretch="Uniform" Margin="0,0,0,4"
                               Cursor="Hand" ToolTip="点击放大查看"
                               MouseLeftButtonUp="QrImage_Click"/>
                        <TextBlock Text="新增工具收集清单（扫码填写）" HorizontalAlignment="Center" Style="{StaticResource InfoContentStyle}"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </ScrollViewer>

        <!-- 底部按钮区域 -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="确定" 
                    Width="80" Height="30" 
                    IsDefault="True"
                    Click="OkButton_Click"
                    Style="{StaticResource ButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>
