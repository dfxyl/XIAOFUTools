<Window x:Class="XIAOFUTools.Features.General.HistoricalImageryDownload.HistoricalVersionSelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="选择下载时间" Height="460" Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        MinHeight="360" MinWidth="360">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/XIAOFUTools;component/Shared/Presentation/Styles/ControlStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="勾选要下载的历史时间："
                   FontWeight="Bold"
                   Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="全选" Width="60" Height="22" Margin="0,0,6,0"
                    Style="{StaticResource DefaultButtonStyle}"
                    Click="SelectAll_Click"/>
            <Button Content="全不选" Width="60" Height="22" Margin="0,0,6,0"
                    Style="{StaticResource DefaultButtonStyle}"
                    Click="SelectNone_Click"/>
            <Button Content="反选" Width="60" Height="22"
                    Style="{StaticResource DefaultButtonStyle}"
                    Click="InvertSelection_Click"/>
        </StackPanel>

        <Border Grid.Row="2" BorderBrush="#CDCDCD" BorderThickness="1" Margin="0,0,0,10">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="6">
                <ItemsControl ItemsSource="{Binding Versions}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <CheckBox Content="{Binding DisplayText}"
                                      IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                      Style="{StaticResource CheckBoxStyle}"
                                      Margin="0,2,0,2"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <TextBlock Grid.Row="3"
                   Margin="0,0,0,10"
                   Text="{Binding SelectionSummary}"
                   Foreground="{StaticResource TextSecondaryBrush}"/>

        <Border Grid.Row="4" BorderBrush="{StaticResource DividerBrush}"
                BorderThickness="0,1,0,0"
                Margin="0,5,0,0"
                Padding="0,10,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="确定" Width="80" Height="22" Margin="0,0,10,0"
                        Style="{StaticResource ExecuteButtonStyle}"
                        Click="Ok_Click"/>
                <Button Content="取消" Width="80" Height="22"
                        Style="{StaticResource CancelButtonStyle}"
                        Click="Cancel_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
