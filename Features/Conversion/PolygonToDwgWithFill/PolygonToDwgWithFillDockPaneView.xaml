<UserControl x:Class="XIAOFUTools.Features.Conversion.PolygonToDwgWithFill.PolygonToDwgWithFillDockPaneView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="520" d:DesignWidth="420"
             Loaded="UserControl_Loaded">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/XIAOFUTools;component/Shared/Presentation/Styles/ControlStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 顶部：基本设置 -->
        <Border Grid.Row="0" Background="White" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="6,6,6,4">
            <GroupBox Header="基本设置" Style="{StaticResource GroupBoxStyle}">
                <Grid Margin="4">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- 面图层选择 -->
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="要素图层(面):"
                               Style="{StaticResource FormLabelStyle}" Margin="0,0,6,4"/>
                    <ComboBox Grid.Row="0" Grid.Column="1" Margin="0,0,3,4"
                              ItemsSource="{Binding PolygonLayers}"
                              SelectedItem="{Binding SelectedPolygonLayer}"
                              DisplayMemberPath="Name"
                              Style="{StaticResource ComboBoxStyle}"/>
                    <Button Grid.Row="0" Grid.Column="2" Content="刷新" Margin="0,0,0,4"
                            Command="{Binding RefreshLayersCommand}"
                            Style="{StaticResource DefaultButtonStyle}"/>

                    <!-- 输出DWG路径 -->
                    <TextBlock Grid.Row="1" Grid.Column="0" Text="输出DWG:"
                               Style="{StaticResource FormLabelStyle}" Margin="0,0,6,0"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Margin="0,0,3,0"
                             Text="{Binding OutputPath}"
                             Style="{StaticResource TextBoxStyle}"/>
                    <Button Grid.Row="1" Grid.Column="2" Content="浏览"
                            Command="{Binding BrowseOutputPathCommand}"
                            Style="{StaticResource DefaultButtonStyle}"/>

                    <!-- DWG 版本选择 -->
                    <TextBlock Grid.Row="2" Grid.Column="0" Text="DWG版本:"
                               Style="{StaticResource FormLabelStyle}" Margin="0,4,6,0"/>
                    <ComboBox Grid.Row="2" Grid.Column="1" Margin="0,4,3,0"
                              ItemsSource="{Binding DwgVersions}"
                              SelectedItem="{Binding SelectedDwgVersion}"
                              DisplayMemberPath="Name"
                              Style="{StaticResource ComboBoxStyle}"/>
                </Grid>
            </GroupBox>
        </Border>

        <!-- 中间：滚动区域（仅用于上方选项与进度） -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="8,6,8,6">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- 选项（预留） -->
                <GroupBox Grid.Row="0" Header="导出选项" Margin="0,0,0,8" Style="{StaticResource GroupBoxStyle}">
                    <Grid Margin="6">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Vertical" Margin="0,0,8,0">
                            <CheckBox Content="导出边界线" IsChecked="{Binding ExportBoundary}" Style="{StaticResource CheckBoxStyle}"/>
                            <CheckBox Content="导出面填充" IsChecked="{Binding ExportHatch}" Style="{StaticResource CheckBoxStyle}"/>
                        </StackPanel>
                        <Grid Grid.Row="0" Grid.Column="1">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Row="0" Grid.Column="0" Text="线宽(mm):" Style="{StaticResource FormLabelStyle}" Margin="0,0,6,3"/>
                            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding LineWidth}" Style="{StaticResource TextBoxStyle}"/>
                            <TextBlock Grid.Row="1" Grid.Column="0" Text="填充透明度(0-100):" Style="{StaticResource FormLabelStyle}" Margin="0,4,6,0"/>
                            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding HatchTransparency}" Style="{StaticResource TextBoxStyle}"/>
                        </Grid>
                        <Button Grid.Row="1" Grid.ColumnSpan="2" Content="命名规则..." Width="110" HorizontalAlignment="Right" Margin="0,8,0,0"
                                Click="OpenNamingDialog_Click"
                                Style="{StaticResource DefaultButtonStyle}"/>
                    </Grid>
                </GroupBox>

                <!-- 进度条 -->
                <ProgressBar Grid.Row="1" Margin="0,0,0,6"
                             Value="{Binding Progress}"
                             Minimum="0" Maximum="100"
                             IsIndeterminate="{Binding IsProgressIndeterminate}"
                             Style="{StaticResource ProgressBarStyle}"/>
            </Grid>
        </ScrollViewer>

        <!-- 日志窗口（可拉伸，自适应高度） -->
        <Border Grid.Row="2" BorderBrush="#CDCDCD" BorderThickness="1" Margin="8,0,8,6">
            <TextBox Text="{Binding LogContent, Mode=OneWay}"
                     Style="{StaticResource LogTextBoxStyle}"
                     BorderThickness="0"
                     VerticalAlignment="Stretch"/>
        </Border>

        <!-- 底部：按钮与状态 -->
        <Border Grid.Row="3" Background="White" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0" Padding="8,6,8,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Content="?" VerticalAlignment="Center" Margin="0,0,8,0"
                        Command="{Binding ShowHelpCommand}"
                        Style="{StaticResource HelpButtonStyle}"/>

                <TextBlock Grid.Column="1" Text="{Binding StatusMessage}" Style="{StaticResource StatusTextStyle}" TextWrapping="Wrap" VerticalAlignment="Center"/>

                <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="停止" Command="{Binding CancelCommand}" Margin="0,0,6,0"
                            Visibility="{Binding IsProcessing, Converter={StaticResource BooleanToVisibilityConverter}}"
                            IsEnabled="{Binding IsProcessing}"
                            Style="{StaticResource CancelButtonStyle}"/>
                    <Button Content="生成DWG" Command="{Binding RunCommand}"
                            IsEnabled="{Binding CanProcess}"
                            Style="{StaticResource ExecuteButtonStyle}"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
