# 更新日志

本文档记录 XIAOFUTools 项目的所有版本更新历史。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

---

## [1.3.0] - 2026-07-18

### 合并
- 将原独立发布的 GIS Toolbox 整合进 XIAOFU工具箱；安装一个 XIAOFUTools.esriAddinX 即可使用原有工具箱功能与 GIS 工具口袋，无需再分别安装两个项目。
- 保留原 GIS Toolbox 的工具箱收纳、命令调用、地图工具、自定义工具组、搜索和完整工具包迁移能力。

### 新增
- GIS 工具口袋以独立功能区标签页提供，可集中添加和管理 .atbx、.tbx、.pyt 工具箱、ArcGIS Pro 内置命令、地图工具及自定义工具组。
- 提供工具搜索、工具箱结构树、拖拽排序、显示/隐藏控制、完整工具包导入导出和最多 32 个动态 Ribbon 入口。
- 驱动制图增加交集表格设置、面积调平、布局表格生成和批量导出联动。

### 修复
- 修复 KML/KMZ 标注字段下拉框无法用鼠标展开，以及字段刷新后选择被重置的问题。
- `MDB批量格式转换` 改用 ArcGIS Pro 3.7 内置 `Convert Personal Geodatabase` 工具，直接输出文件地理数据库、移动地理数据库或 XML Workspace Document，移除原自制转换链路。
- AI 助手内置 NVIDIA 服务更新为 MiniMax M3 与 GLM 5.2，移除 Kimi；两者统一支持 100 万 Token 上下文，MiniMax M3 支持多模态输入。
- 修复 AI 助手在流式响应终包或结构化文本分段中未提取显示文本时，错误显示 `finish_reason=stop` 空响应的问题。

### 改进
- 主设置新增 GIS 工具口袋标签页显示开关，可按使用习惯即时显示或隐藏该标签页。
- 工具口袋配置统一保存至 XIAOFUTools 用户数据目录，并兼容读取原 GIS Toolbox 配置。
- 极小“其他”交集面积不再生成无意义表格行，并按保留位数规则并入有效分类。
- AI 助手移除内置默认 API、密钥和模型，升级时清理旧内置配置并保留用户自定义模型。
- 移除主工具运行时的地理处理自检、临时 GDB 创建和界面测试绑定日志，正式操作不再执行开发调试流程。
- 将适合后台执行的地理处理任务切换至 ArcGIS Pro GP 线程，减少大型数据处理、数据库操作与栅格任务对界面操作的影响。
- 优化常用数据扫描与数据库刷新流程，减少同步等待；完善图幅数据工作空间释放，降低批量操作后的数据锁占用。

### 兼容性
- 保持 ArcGIS Pro 3.7、Windows x64 与 .NET 10 支持基线。

### 维护
- 完成单工程发布整理、自动化验证基线调整和公共界面基础能力统一。

---

## [1.2.8] - 2026-05-19

### 变更
- AI 助手移除内置默认API、密钥和模型，首次使用需在设置中手动添加模型配置。
- 升级初始化时自动清理旧版内置AI服务配置，保留用户自定义模型。
- 同步提升 AddInInfo、版本显示和在线更新清单版本号，确保新版安装按更高版本覆盖旧版插件。

---

## [1.2.7] - 2026-03-19

### 新增
- 新增 `历史影像下载` 工具，支持查询 Google 历史影像与 Esri Wayback 版本，按 `查询历史 / 查询全部` 模式批量勾选导出 GeoTIFF，并支持当前视图、框选范围、面图层范围、输出坐标系与下载后自动加载。
- 新增 `互联网切片下载` 工具，支持 `KVP WMTS`、`RESTful WMTS`、`XYZ`、`TMS`、`ArcGIS REST tile`、`QuadKey` 与子域名模板，兼容天地图、ArcGIS Online、Google 风格模板、吉林一号等常见服务链接。
- 新增 `大比例图幅赋值` 与 `小比例图幅赋值` 工具，支持图幅编号批量写入、连续地图查询、结果闪烁定位与主动退出查询。
- 新增独立 `快捷添加数据` 面板，支持常用数据分组管理、批量加载、GDB 结构树展开、拖拽排序与右键导入。

### 改进
- `特殊坐标转换` 升级为 `单层 / 批量 / gdb` 三种模式，补充批量 SHP/GDB 扫描、选择控制、统一进度日志、帮助与停止流程，并增强要素数据集、字段过滤和批量写入稳定性。
- `历史影像` 与 `历史影像下载` 统一采用 Wayback 真实变化判定逻辑，支持优先从当前地图已加载的 Wayback 图层解析版本来源，并补充多来源选择与帮助说明。
- `MDB批量转GDB` 转换链路切换为 ArcGIS Pro Python 引擎，优化要素集识别、中文名称/字段别名兜底与元数据兼容处理，尽量保留原有要素数据集结构。
- `批量添加数据` 复用快捷数据面板的扫描与加载服务，统一数据识别、拖拽加载与收藏管理体验。

### 修复
- 修复互联网切片与历史影像下载输出临时 GeoTIFF 时文件句柄未及时释放的问题，避免导出后文件被占用。
- 修复 WMTS 坐标系别名识别、天地图 `TopLeftCorner` 方向异常与矩阵分辨率计算问题，避免“下载范围超出服务覆盖范围”或导出影像空间位置偏移。
- 修复 `互联网切片下载` 与 `历史影像下载` 框选完成后地图持续停留在草图状态的问题，统一恢复为浏览工具。
- 修复图幅查询重复启动后的连续使用问题、重复订阅导致的日志异常，并在批量赋值时仅提交字段值发生变化的图斑。

### 注意
- 该版本仅支持 ArcGIS Pro 3.6+

---

## [1.2.6] - 2026-03-09

### 新增
- Ribbon 结构整理为 `通用 / 编辑 / 数据 / 制图 / 系统` 5 组，并同步更新 README、用户手册与开发规范文档。
- 新增 `MDB批量转GDB` 工具，支持遍历子目录、批量勾选、输出规划、覆盖确认、停止取消、进度日志与基于 ArcGIS Pro Python 引擎的批量迁移。
- 新增 `批量合并SHP` 工具，支持按点/线/面快速筛选、几何类型校验、字段并集合并与 `MERGE_SRC` 源文件名写入。
- 新增 `文档批量替换` 工具，支持 Word 多文件批量替换、多规则匹配选项、拖拽导入与另存为副本/覆盖原文件。
- 新增 `属性表建SHP`、`SHP输字段表`、`提取面扣岛`、`数据透视` 等工具，补齐数据整备与分析检查链路。

### 修复
- 修复 `MDB批量转GDB` 中文名称/字段值乱码、编码检测性能问题，以及转换运行时打包问题。
- 修复 `要素类转TXT` 面积单位与默认编号/名称兜底逻辑，避免错误输出平方米或误用系统 OID。
- 修复资源路径错配与 `图片转PDF` 依赖问题，将实现切换到 `PDFsharp-GDI` 并消除无效资源项。
- 修复 `计算面积` 从右键菜单打开时的上下文图层传参问题，改为 URI 优先匹配并补齐可访问性修正。
- 修复 `要素图层转KML/KMZ` 导出兼容性、字段标注一致性与导出性能问题。
- 修复 `数据透视` 连续 GP 调用下 `in_memory` 临时表失效导致的 `ERROR 000732`。

### 改进
- `属性表建库` 新增坐标系设置项，增强中文几何/字段类型兼容，并升级两套 Excel 模板说明与校验。
- `驱动制图` 新增页面搜索、筛选结果批量选择能力，并优化界址点表压缩、省略标记与边长显示逻辑。
- 更新 `数据透视` 与 `文档批量替换` 专用图标资源，统一工具分组与界面识别度。

### 注意
- 该版本仅支持 ArcGIS Pro 3.6+

---

## [1.2.5] - 2026-02-09

### 新增
- AI 助手重构为分层架构，新增工具请求解析、提示词管理与应用编排能力
- AI 助手新增 GIS 智能工具链：图层列表、图层查询、图层缓冲、图层裁剪、字段画像、图层结构、压盖汇总、选中摘要、项目快照、联网抓取

### 改进
- 多模块功能与界面交互优化，覆盖分析、转换、数据处理、编辑与用户工具等核心模块
- 统一更新图标与界面样式资源，提升工具栏视觉一致性与识别性
- 修复若干bug

### 注意
- 该版本仅支持 ArcGIS Pro 3.6+

---

## [1.2.4] - 2026-02-04

### 新增
- 要素图层分组导出KML/KMZ工具 - 支持按字段分组导出、自动读取符号样式、生成独立文字标注层

### 改进
- 驱动制图页面加载性能优化，加载速度提升10-100倍

### 注意
- 该版本仅支持 ArcGIS Pro 3.6+

---

## [1.2.3] - 2026-02-04

### 新增
- 布局元素查找替换工具 - 类似Excel的批量查找替换文字功能

### 改进
- 驱动制图页面加载性能优化
- 输出按钮面板更名为制图相关

### 注意
- 该版本仅支持 ArcGIS Pro 3.6+

---

## [1.2.2] - 2025-12-26

### 新增
- 多图层压盖汇总 - 支持多压盖图层交集面积统计与导出
- 地图生成界址点线 - 支持布局图形图层生成界址点/线/标注

### 改进
- 交集汇总表工具优化，支持多字段统计与 Excel 导出

### 修复
- 修复 TXT 转 SHP "保存在 txt 源路径" 选项报错

### 注意
- 该版本仅支持 ArcGIS Pro 3.6+

---

## [1.2.1] - 2025-12-20

### 新增
- 驱动制图工具 - 基于 Map Series 功能实现批量导图
- 属性表建库工具 - 根据 Excel 属性结构表自动创建文件地理数据库
- 镜像数据库工具 - 将源数据库的结构镜像到新数据库

### 改进
- 工具箱结构与界面布局调整

### 注意
- 该版本仅支持 ArcGIS Pro 3.6+

---

## [1.2.0] - 2025-12-15

### 首个正式版本
- 发布 XIAOFU工具箱基础功能集
- 支持常用 GIS 数据处理、分析与制图辅助工具
